using System;

namespace ArxisStudio.Markup.Xaml.Loader.Sample;

/// <summary>
/// Demonstrates library usage only. This sample must never grow into a visual designer.
/// </summary>
internal static class Program
{
    private static void Main()
    {
        Console.WriteLine("ArxisStudio.Markup.Xaml.Loader sample.");
        Console.WriteLine("The public API arrives with the milestones; see README.md.");
    }
}
