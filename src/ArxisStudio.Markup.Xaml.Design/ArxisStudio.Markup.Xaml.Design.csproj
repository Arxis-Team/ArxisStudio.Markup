<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>ArxisStudio.Markup.Xaml.Design</RootNamespace>
    <Description>Makes a loaded document viewable when its root cannot be shown: a stand-in control that projects a TopLevel root's resources, styles, background, theme variant and size, and publishes its chrome as data for a host to draw.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../ArxisStudio.Markup.Xaml.Loader/ArxisStudio.Markup.Xaml.Loader.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" />
  </ItemGroup>

</Project>
