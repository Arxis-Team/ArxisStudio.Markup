<Project>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <!-- Quality gates. The contract requires warnings to be treated as errors. -->
  <PropertyGroup>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <WarningLevel>9999</WarningLevel>
    <EnableNETAnalyzers>true</EnableNETAnalyzers>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
  </PropertyGroup>

  <!--
    Carry the configuration into project references.

    MSBuild decides a reference's configuration from the configuration table of the solution being
    built, and defaults to *unsetting* it for any project that table does not mention. Building this
    repository through its own solution that never happens, because every project is in it. Building
    it as a referenced dependency of another repository's solution, none of them is: the reference
    that enters here keeps whatever configuration it was given, and every reference below it falls
    back to the default and builds Debug.

    The result is a Release build that quietly links Debug assemblies, which is why this is set
    here rather than worked around by whoever is consuming the repository. A consumer cannot reach
    inside this project graph to fix it.
  -->
  <PropertyGroup>
    <ShouldUnsetParentConfigurationAndPlatform>false</ShouldUnsetParentConfigurationAndPlatform>
  </PropertyGroup>

  <!--
    Nothing here is published. The three libraries under src/ are consumed by project reference,
    so `dotnet pack` should produce nothing rather than three packages nobody asked for — and
    saying so once here is better than four copies of it saying so per directory.
  -->
  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <PropertyGroup>
    <Company>ArxisStudio</Company>
    <Product>ArxisStudio.Markup</Product>
    <Copyright>Copyright (c) ArxisStudio</Copyright>
  </PropertyGroup>

</Project>
